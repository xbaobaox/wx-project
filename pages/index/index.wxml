<view class="container">
<view class="nav_bar">
  <text wx:for="{{nav}}" 
    wx:key="{{index}}"
    data-index="{{index}}"
    bindtap="selectedNav" 
    class="{{index==x ? 'selectedItem':''}}">
   {{item.name}}</text>
</view>
</view>

